[project]
name = "to-do"
version = "0.1.0"
description = "App for tasks"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.12",
    "uvicorn>=0.34.2",
    "asyncpg>=0.30.0",
    "pydantic-settings>=2.0.0",
    "python-dotenv>=1.0.0"
]
